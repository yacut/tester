'use babel';

/* @flow */
describe('output-view', () => {
  it('should write more tests', () => {
    expect('i should write more tests').toNotEqual('this component has a lot of tests :)');
  });
});
